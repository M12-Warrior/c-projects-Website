# Cursor / product plans

These files are copies of planning notes (originally from Cursor’s plan workflow under `C:\Users\truck\.cursor\plans\` and from `/.cursor/plans/` in this repo). They are documentation only; the running app does not depend on them.

Update here when product decisions change, then commit so GitHub stays the source of truth alongside code.
